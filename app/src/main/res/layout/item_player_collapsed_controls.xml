<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/player_collapsed_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    android:background="@drawable/shape_miniplayer_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/collapsed_cover_size"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/collapsedTrackCover"
            android:layout_width="@dimen/collapsed_cover_size"
            android:layout_height="@dimen/collapsed_cover_size"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.Collapsed.Cover" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="16dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/collapsedTrackTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="marquee"
                android:maxLines="1"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/collapsedTrackArtist"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="marquee"
                android:maxLines="1"
                android:textColor="?attr/colorOnSurface" />

        </LinearLayout>

        <!-- Reserves the trailing play/pause control's width so the weighted title/artist column above
             doesn't slide under it. Mirrored in item_player_collapsed.xml (the collapse-animation twin);
             both use @dimen/collapsed_end_controls_width so they stay in sync. -->
        <View
            android:layout_width="@dimen/collapsed_end_controls_width"
            android:layout_height="@dimen/collapsed_cover_size" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="@dimen/collapsed_cover_size"
        android:layout_gravity="end"
        android:padding="8dp">

        <FrameLayout
            android:id="@+id/collapsedPlayPauseContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center">

            <com.google.android.material.progressindicator.CircularProgressIndicator
                android:id="@+id/collapsedPlayingIndicator"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:clickable="false"
                android:indeterminate="true"
                app:indicatorSize="36dp" />

            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/collapsedTrackPlayPause"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/ripple_44dp"
                android:button="@drawable/anim_play_pause_32dp"
                android:contentDescription="@string/play"
                app:buttonTint="@color/button_player" />

        </FrameLayout>

    </LinearLayout>

    <!-- Despite the id, this now draws ONLY the inactive rail: its buffer indicator is deliberately
         transparent, matching bufferBar in item_player_controls.xml and tv_buffer_bar in
         fragment_player_tv.xml. Keep all three in step.
         BUFFERING IS NOW SHOWN NOWHERE IN THE APP. The full screen and TV players lost it in build 1055
         because DeterminateDrawable never assigns startFraction (it stays 0f), so a LinearProgressIndicator
         can only fill from the LEFT EDGE: with buffered >= position that painted a solid accent line under
         the whole played portion, which showed through the wavy seek bar wherever the band swung clear of
         the centre line. The mini bar has no wave, so it kept drawing its buffer line and was the only
         surface still showing one. That inconsistency is what this removes.
         If you restore it, restore it on ALL THREE surfaces or none, and read the note on bufferBar first:
         the artefact it causes on the wavy bar is real and there is no way to draw only the useful
         playhead->buffered segment from one indicator (ProgressBar.min rescales the fraction, it does not
         offset the start).
         THIS VIEW STAYS. It is the rail carrier, and it owns three things collapsed_seekbar does not:
         android:alpha 0.5 (the rail is half opacity), indicatorTrackGapSize 0dp (no break at the playhead),
         and the trackColor that PlayerFragment tints at runtime. alpha in particular CANNOT move to
         collapsed_seekbar, because android:alpha is view level and would dim the position line with it.
         fillTrack() is called with (0f, 1f) unconditionally, so the rail stays continuous across the full
         width with the indicator transparent - no gap where the fill used to be.
         progress and max are still written in PlayerFragment (:621, :646); they are inert while the
         indicator is transparent and are kept so a one line colour change brings the display back. -->
    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/collapsed_buffer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:alpha="0.5"
        android:progress="40"
        app:indicatorColor="@android:color/transparent"
        app:indicatorTrackGapSize="0dp"
        app:trackColor="?attr/colorOnSurface"
        app:trackThickness="2dp" />

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/collapsed_seekbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:progress="20"
        app:indicatorColor="?attr/colorTertiary"
        app:trackColor="@android:color/transparent"
        app:trackThickness="2dp" />
</FrameLayout>